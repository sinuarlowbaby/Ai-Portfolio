# ─── Environment Configuration ────────────────────────────────────────────────
# Copy this file to .env and fill in the values before running in production.
# Never commit .env to version control.

# Application environment: "development" or "production"
ENV=development

# Database URL (SQLite for dev, PostgreSQL for prod)
DATABASE_URL=sqlite:///./portfolio.db
# DATABASE_URL=postgresql://user:password@localhost:5432/portfolio

# CORS — comma-separated list of allowed frontend origins (production only)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Trusted hosts — comma-separated (production only, prevents Host header attacks)
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
